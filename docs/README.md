# Documentation Structure

- `docs/active/`: Current operational and technical docs used for deployment, integrations, and runbooks.
- `docs/archive/`: Historical notes, old checklists, and one-off investigation records kept for reference.

When a document is no longer part of active operations, move it from `docs/active/` to `docs/archive/` instead of deleting it.
